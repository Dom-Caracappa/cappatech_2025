# main-site/.env

# 🚀 Astro server port (during local dev)
PORT=4321

# Frontend public URL (used for CORS or external redirects if needed)
PUBLIC_FRONTEND_URL=https://yourdomain.com  # update this in production

# Full URL to your external API (Express backend)
PUBLIC_API_URL=https://api.yourdomain.com  # update this in production

# Used by Express API backend only (not exposed to frontend)
DATABASE_URL=file:./server/db/contact-form.db


PUBLIC_API_URL=http://localhost:5042
