# PORT your Express API will run on
PORT=5042

# Used for CORS and redirects in the Express server
FRONTEND_URL=http://localhost:4321

# SQLite DB path — used in drizzle.mts or wherever Drizzle connects
DATABASE_URL=file:./server/db/contact-form.db
